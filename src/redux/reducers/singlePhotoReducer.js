const initialState = {
  singlePhoto: {},
  loading: true,
  error: false,
};

const singlePhotoReducer = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default singlePhotoReducer;
